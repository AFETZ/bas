Ad-hoc diagnostics live here.

These helpers are intentionally kept out of the main `scripts/` surface because
they are one-off probes for local SITL, OptiX, ArduPilot build, and parser
debugging sessions. Prefer promoting a helper back to `scripts/` only after it
becomes a documented, repeatable smoke or demo entrypoint.
